-- Признак того, что игрок записался сам, нажав «Записаться» в боте.
--
-- Раньше галочка в списке ставилась за сам факт наличия регистрации, но записи
-- создаёт и администратор через «Быстрое добавление». Из-за этого список, который
-- должен показывать «кто ДОЛЖЕН быть на тренировке», помечал подтвердившими всех
-- подряд.
--
-- Существующие строки получают false: достоверно отличить, кто из них записался сам,
-- а кого внёс администратор, задним числом нельзя, а ложная галочка хуже её
-- отсутствия — игроки просто подтвердят заново.

BEGIN;

ALTER TABLE registrations
    ADD COLUMN IF NOT EXISTS self_registered BOOLEAN NOT NULL DEFAULT false;

COMMIT;
